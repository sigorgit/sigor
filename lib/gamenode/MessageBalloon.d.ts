import { GameNode } from "skydapp-browser";
export default class MessageBalloon extends GameNode {
    private balloonColor;
    private content;
    private delay;
    constructor(y: number);
    updateMessage(message: string): void;
}
//# sourceMappingURL=MessageBalloon.d.ts.map